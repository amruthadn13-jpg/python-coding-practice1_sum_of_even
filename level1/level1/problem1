user_input=input("Enter a list of numbers: ")
numbers=list(map(int,user_input.split()))
def sum_of_numbers(num):
    total=0
    for n in  num:
        if n%2==0:
            total+=n
    return total
print("sum_of_numbers: ",sum_of_numbers(numbers))
